# ========================
# APPLICATION
# ========================
spring.application.name=zidio
server.port=8080

# ========================
# DATABASE
# ========================
spring.datasource.url=jdbc:mysql://localhost:3306/zidio_connection?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Saranpavi1529@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ========================
# JWT CONFIGURATION
# ========================
jwt.secret=ZidioSecretKeyForJWTGeneration1234567890
jwt.expiration-ms=18000000

# ========================
# LOGGING
# ========================
logging.level.org.springframework.security=DEBUG

# ========================
# EMAIL (GMAIL SMTP TEST ACCOUNT)
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=saransmb2@gmail.com
spring.mail.password=klfhqfhempvredme
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ========================
# EUREKA CLIENT
# ========================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.lease-renewal-interval-in-seconds=5
eureka.instance.lease-expiration-duration-in-seconds=15

# ========================
# ACTUATOR
# ========================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# Optional: Disable DB check temporarily if your DB is down
# management.health.db.enabled=false

# ========================
# CLOUDINARY (TEST KEYS)
# ========================
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

# ========================
# RAZORPAY (TEST KEYS)
# ========================
razorpay.key.id=rzp_test_ABC123456789
razorpay.key.secret=xyz987654321
razorpay.test-mode=true 

# ========================
# OTHER CONFIGS
# ========================
spring.main.allow-bean-definition-overriding=true

# ========================
# MULTIPART FILE UPLOAD
# ========================
# Enable multipart support
spring.servlet.multipart.enabled=true

# Maximum file size (5 MB)
spring.servlet.multipart.max-file-size=5MB

# Maximum request size (5 MB)
spring.servlet.multipart.max-request-size=5MB
